{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.js" 
      // This ensures API routes still go to your Express app
    }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}